# Via ts-jest

# ts-jest is a TypeScript preprocessor with source map support for Jest that 
# lets you use Jest to test projects written in TypeScript.

npm install --save-dev ts-jest

# In order for Jest to transpile TypeScript with ts-jest, you may also need
# to create a configuration file.